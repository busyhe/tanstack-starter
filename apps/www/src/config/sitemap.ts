/**
 * Public, indexable routes included in the generated sitemap.
 * Keep this list in sync when adding or removing public page routes.
 */
export const sitemapPages = [
  {
    path: '/',
    sitemap: { changefreq: 'weekly' as const, priority: 1 },
  },
]
